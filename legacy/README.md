# Legacy PowerShell

This folder keeps the original PowerShell + WPF app (`ConsoleMode.ps1`, `lib\`, `Build-ConsoleMode.ps1`) as a reference while the product moves to C# / WinUI 3.

The current app lives in `src/ConsoleMode`. Do not add new features here.
