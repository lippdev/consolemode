<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ConsoleMode.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:ConsoleMode.ViewModels"
    Title="Console Mode">

    <Grid x:Name="RootGrid" Background="{StaticResource BgBrush}" Padding="26,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,4,0,10">
            <Border Width="46" Height="46" CornerRadius="12" Background="{StaticResource AccentDarkBrush}">
                <TextBlock Style="{StaticResource IconText}" Text="&#xE7F4;" FontSize="22"
                           Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Console Mode" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="Transforme seu PC em console de jogos em poucos passos."
                           Foreground="{StaticResource MutedBrush}" Margin="1,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource TabBtn}"
                    Command="{x:Bind ViewModel.GoTabCommand}" CommandParameter="0"
                    Background="{Binding Tab0Active, Converter={StaticResource TabBg}}"
                    Foreground="{Binding Tab0Active, Converter={StaticResource TabFg}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE7F4;" FontSize="15" Margin="0,0,8,0"/>
                    <TextBlock Text="1. Monitores" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="1" Style="{StaticResource TabBtn}"
                    Command="{x:Bind ViewModel.GoTabCommand}" CommandParameter="1"
                    Background="{Binding Tab1Active, Converter={StaticResource TabBg}}"
                    Foreground="{Binding Tab1Active, Converter={StaticResource TabFg}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE7FC;" FontSize="15" Margin="0,0,8,0"/>
                    <TextBlock Text="2. Modo" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="2" Style="{StaticResource TabBtn}"
                    Command="{x:Bind ViewModel.GoTabCommand}" CommandParameter="2"
                    Background="{Binding Tab2Active, Converter={StaticResource TabBg}}"
                    Foreground="{Binding Tab2Active, Converter={StaticResource TabFg}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE767;" FontSize="15" Margin="0,0,8,0"/>
                    <TextBlock Text="3. Áudio" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="3" Style="{StaticResource TabBtn}"
                    Command="{x:Bind ViewModel.GoTabCommand}" CommandParameter="3"
                    Background="{Binding Tab3Active, Converter={StaticResource TabBg}}"
                    Foreground="{Binding Tab3Active, Converter={StaticResource TabFg}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE768;" FontSize="15" Margin="0,0,8,0"/>
                    <TextBlock Text="Iniciar" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2">
            <ScrollViewer Visibility="{x:Bind ViewModel.Step0Visibility, Mode=OneWay}">
                <StackPanel>
                    <Border Style="{StaticResource Card}" Padding="16">
                        <StackPanel>
                            <Grid Margin="4,0,4,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="52"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="240"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Foco" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Esconder" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="Monitor" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold" Margin="8,0,0,0"/>
                                <TextBlock Grid.Column="3" Text="Resolução / Hz" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold"/>
                            </Grid>
                            <ItemsControl ItemsSource="{x:Bind ViewModel.Monitors, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="vm:MonitorRowViewModel">
                                        <Border Style="{StaticResource Card}" Padding="12,10" Margin="0,0,0,8" Background="{StaticResource CardBrush}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="52"/>
                                                    <ColumnDefinition Width="80"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="240"/>
                                                </Grid.ColumnDefinitions>
                                                <RadioButton Grid.Column="0" GroupName="focusMonitor" VerticalAlignment="Center"
                                                             IsChecked="{x:Bind IsFocus, Mode=TwoWay}"/>
                                                <CheckBox Grid.Column="1" VerticalAlignment="Center"
                                                          IsChecked="{x:Bind IsHide, Mode=TwoWay}"/>
                                                <TextBlock Grid.Column="2" Text="{x:Bind Title}" VerticalAlignment="Center"
                                                           Foreground="{StaticResource TextBrush}" Margin="8,0,8,0" TextWrapping="Wrap"/>
                                                <ComboBox Grid.Column="3" HorizontalAlignment="Stretch"
                                                          ItemsSource="{x:Bind Modes}"
                                                          SelectedItem="{x:Bind SelectedMode, Mode=TwoWay}"
                                                          DisplayMemberPath="Text"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Grid Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Style="{StaticResource Btn}" Height="38" Command="{x:Bind ViewModel.HideOthersCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Style="{StaticResource IconText}" Text="&#xED1A;" Margin="0,0,8,0"/>
                                        <TextBlock Text="Esconder todos exceto o foco"/>
                                    </StackPanel>
                                </Button>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource IconText}" Text="&#xE946;" Margin="0,2,8,0" VerticalAlignment="Top"/>
                                    <TextBlock Text="Desconectados: use modos do cache ou estimados — aplicados ao conectar. Serão reativados ao iniciar."
                                               Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" FontSize="12" MaxWidth="420"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}" Padding="16" Margin="0,12,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xEC4A;" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
                                <TextBlock Text="Limite de FPS (anti-tearing)" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ComboBox Width="260"
                                          ItemsSource="{x:Bind ViewModel.FpsOptions, Mode=OneWay}"
                                          SelectedItem="{x:Bind ViewModel.SelectedFps, Mode=TwoWay}"
                                          DisplayMemberPath="Text"/>
                                <TextBox Width="90" Margin="10,0,0,0"
                                         Visibility="{Binding ShowCustomFps, Converter={StaticResource BoolToVis}}"
                                         Text="{x:Bind ViewModel.CustomFpsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="2,10,0,0"
                                       Text="{x:Bind ViewModel.FpsStatusText, Mode=OneWay}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer Visibility="{x:Bind ViewModel.Step1Visibility, Mode=OneWay}">
                <StackPanel>
                    <Border Style="{StaticResource Card}" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Como esconder os outros monitores?" Foreground="{StaticResource AccentBrush}"
                                       FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <ComboBox HorizontalAlignment="Stretch"
                                      ItemsSource="{x:Bind ViewModel.HideStrategies, Mode=OneWay}"
                                      SelectedItem="{x:Bind ViewModel.SelectedHideStrategy, Mode=TwoWay}"
                                      DisplayMemberPath="Text"/>
                            <TextBlock Text="Desconectar: desativa no Windows (rápido). Cortinas: overlay preto. DDC/CI: apaga o painel via hardware."
                                       Foreground="{StaticResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="2,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}" Padding="16" Margin="0,12,0,0">
                        <StackPanel>
                            <TextBlock Text="Modo de tela cheia" Foreground="{StaticResource AccentBrush}"
                                       FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <RadioButton GroupName="fsmode" IsChecked="{x:Bind ViewModel.ModeBigPicture, Mode=TwoWay}">
                                <TextBlock Text="Steam Big Picture (recomendado)" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                            </RadioButton>
                            <TextBlock Text="Abre a Steam em modo Big Picture no monitor de foco. Restauração automática ao sair."
                                       Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="30,4,0,14"/>
                            <RadioButton GroupName="fsmode" IsChecked="{x:Bind ViewModel.ModePlaynite, Mode=TwoWay}">
                                <TextBlock Text="Playnite (modo tela cheia)" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                            </RadioButton>
                            <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="30,4,0,14"
                                       Text="{x:Bind ViewModel.PlayniteDesc, Mode=OneWay}"/>
                            <RadioButton GroupName="fsmode" IsChecked="{x:Bind ViewModel.ModeXbox, Mode=TwoWay}">
                                <TextBlock Text="Modo Xbox (Win+F11) — Alpha" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                            </RadioButton>
                            <TextBlock Text="Experimental: envia Win+F11. O app permanece aberto; restaure manualmente com Restaurar agora."
                                       Foreground="{StaticResource WarningBrush}" FontSize="12" Margin="30,4,0,2"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}" Padding="16" Margin="0,12,0,0">
                        <StackPanel>
                            <TextBlock Text="Recursos de vídeo" Foreground="{StaticResource AccentBrush}"
                                       FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Content="Ativar HDR no monitor de foco"
                                          IsChecked="{x:Bind ViewModel.HdrEnable, Mode=TwoWay}"/>
                                <CheckBox Grid.Column="1" Content="Ativar VRR (taxa de atualização variável)"
                                          IsChecked="{x:Bind ViewModel.VrrEnable, Mode=TwoWay}"/>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE7BA;" Foreground="{StaticResource WarningBrush}"
                                           FontSize="12" Margin="0,2,8,0" VerticalAlignment="Top"/>
                                <TextBlock Text="VRR: esta opção aplica apenas o ajuste do Windows — recomendamos ligar o VRR pelo painel do driver da GPU (NVIDIA/AMD)."
                                           Foreground="{StaticResource WarningBrush}" FontSize="12" TextWrapping="Wrap" MaxWidth="620"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Visibility="{x:Bind ViewModel.Step2Visibility, Mode=OneWay}">
                <Border Style="{StaticResource Card}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Saída de áudio" Foreground="{StaticResource AccentBrush}"
                                   FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ComboBox HorizontalAlignment="Stretch"
                                  ItemsSource="{x:Bind ViewModel.AudioOptions, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedAudio, Mode=TwoWay}"
                                  DisplayMemberPath="Text"/>
                        <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="2,10,0,0"
                                   Text="{x:Bind ViewModel.AudioHintText, Mode=OneWay}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Visibility="{x:Bind ViewModel.Step3Visibility, Mode=OneWay}">
                <Border Style="{StaticResource Card}" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <TextBlock Style="{StaticResource IconText}" Text="&#xE930;" Foreground="{StaticResource AccentBrush}"
                                       FontSize="18" Margin="0,0,10,0"/>
                            <TextBlock Text="Revisão" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                        </StackPanel>
                        <TextBlock Foreground="{StaticResource TextBrush}" FontSize="14" TextWrapping="Wrap"
                                   Text="{x:Bind ViewModel.ReviewText, Mode=OneWay}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Visibility="{x:Bind ViewModel.ActivePanelVisibility, Mode=OneWay}">
                <Border Style="{StaticResource Card}" Padding="24">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Style="{StaticResource IconText}" Text="&#xE768;" Foreground="{StaticResource AccentBrush}"
                                       FontSize="20" Margin="0,0,10,0"/>
                            <TextBlock Text="Modo console ativo" FontSize="19" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                        </StackPanel>
                        <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="14" TextWrapping="Wrap"
                                   Text="{x:Bind ViewModel.ActiveDescText, Mode=OneWay}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="4,14,0,12">
            <TextBlock Style="{StaticResource IconText}" Text="{x:Bind ViewModel.StatusIcon, Mode=OneWay}" FontSize="14"
                       Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
            <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" Foreground="{StaticResource MutedBrush}"/>
        </StackPanel>

        <Grid Grid.Row="4" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource Btn}" Width="120"
                    Command="{x:Bind ViewModel.BackCommand}"
                    IsEnabled="{x:Bind ViewModel.CanGoBack, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE76B;" FontSize="12" Margin="0,0,8,0"/>
                    <TextBlock Text="Voltar"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="1" Style="{StaticResource BtnPrimary}" Width="130"
                    Command="{x:Bind ViewModel.SaveCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE74E;" FontSize="13" Margin="0,0,8,0"
                               Foreground="{StaticResource DarkTextBrush}"/>
                    <TextBlock Text="Salvar"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="2" Style="{StaticResource Btn}" Width="140"
                    Command="{x:Bind ViewModel.RefreshCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE72C;" FontSize="13" Margin="0,0,8,0"/>
                    <TextBlock Text="Atualizar"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="3" Style="{StaticResource Btn}" Width="170"
                    Command="{x:Bind ViewModel.RestoreNowCommand}"
                    IsEnabled="{x:Bind ViewModel.CanRestore, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE7A7;" FontSize="13" Margin="0,0,8,0"/>
                    <TextBlock Text="Restaurar agora"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="5" Style="{StaticResource BtnPrimary}" Width="150"
                    Visibility="{Binding ShowNext, Converter={StaticResource BoolToVis}}"
                    Command="{x:Bind ViewModel.NextCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Próximo"/>
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE76C;" FontSize="12" Margin="8,0,0,0"
                               Foreground="{StaticResource DarkTextBrush}"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="5" Style="{StaticResource BtnPrimary}" Width="230"
                    Visibility="{Binding ShowStart, Converter={StaticResource BoolToVis}}"
                    Command="{x:Bind ViewModel.StartCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconText}" Text="&#xE768;" FontSize="13" Margin="0,0,8,0"
                               Foreground="{StaticResource DarkTextBrush}"/>
                    <TextBlock Text="Iniciar modo console" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
